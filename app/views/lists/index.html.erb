<%= render 'navbar' %>

<div class='container'>
  <h1>My lists</h1>
      <% @lists.each do |list| %>
        <div class="card" style="width: 18rem;">
          <div class="card-body">
            <%= link_to (list.name), list_path(list.id) %>
          </div>
        </div>
      <% end %>
</div>
